Asset and network marks for wallet, balance and transfer rows.

```jsx
<CryptoIcon symbol="Bitcoin" size={24} />
<CryptoIcon symbol="Polygon" shape="square" size={20} />
```

`shape="round"` is the cryptocurrency mark, `shape="square"` the network mark. Read `CryptoIcon.d.ts` for the 47 available symbols.
