import CryptoIcon from './CryptoIcon.jsx';

/* Figma: Crypto Icons Round - Cryptocurrencies (44 variants). The Figma family name, exported verbatim so the
   family maps one-to-one onto a component. CryptoIconRound is the shorter alias. */

export function CryptoIconsRoundCryptocurrencies(props) {
  return <CryptoIcon {...props} shape="round" />;
}
export default CryptoIconsRoundCryptocurrencies;
