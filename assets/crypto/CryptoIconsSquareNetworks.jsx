import CryptoIcon from './CryptoIcon.jsx';

/* Figma: Crypto Icons Square - Networks (47 variants). The Figma family name, exported verbatim so the
   family maps one-to-one onto a component. CryptoIconSquare is the shorter alias. */

export function CryptoIconsSquareNetworks(props) {
  return <CryptoIcon {...props} shape="square" />;
}
export default CryptoIconsSquareNetworks;
