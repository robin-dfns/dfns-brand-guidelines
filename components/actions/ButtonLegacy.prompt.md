The file's older 40px button. Prefer `Button` for new work; use this only when matching a screen built against the earlier set.

```jsx
<ButtonLegacy tone="primary">Send</ButtonLegacy>
<ButtonLegacy tone="outline" variant="Hover">Export History</ButtonLegacy>
```
