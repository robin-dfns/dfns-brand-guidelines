DFNS logotype from real brand SVGs — never redraw the mark.

```jsx
import { Logotype } from './Logotype.jsx';
<Logotype height={28} />          // cube + wordmark
<Logotype variant="symbol" height={32} />  // cube only
```

Assets: `assets/logotype-2026.svg` (current 2026 wordmark), `assets/dfns-logo-cube.svg` (cube), plus `assets/logotype-symbol.svg` / `assets/logotype-type.svg` extracted from the Figma Logotype component.
