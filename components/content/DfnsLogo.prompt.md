DFNS colored cube mark from the real brand SVG (`assets/dfns-logo-cube.svg`; `withWordmark` uses `assets/logo.svg`). Never redraw.

```jsx
import { DfnsLogo } from './DfnsLogo.jsx';
<DfnsLogo height={32} base="../../" />
```
