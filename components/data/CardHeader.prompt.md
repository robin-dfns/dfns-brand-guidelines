Opens every card and panel in the dashboard. Title is Title Case, description is Sentence case.

```jsx
<CardHeader title="Recent Transfers" description="Activity across all wallets in this organisation." trailing={<Button variant="ghost" size="sm">View All Activities</Button>} />
```
