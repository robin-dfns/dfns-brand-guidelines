DFNS status badge: tinted pill, radius 6, padding 2×10, 12px, 1px inner ring.

```jsx
import { Badge } from './Badge.jsx';
<Badge color="green">Confirmed</Badge> <Badge color="red">Failed</Badge>
```

Colors: default (purple), blue (info), green (success), red (danger), yellow (warning), grey (neutral).
