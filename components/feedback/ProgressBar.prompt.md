DFNS progress bar: 12px tall, radius 50, track #edf2f7, fill linear-gradient(268deg,#68dbf2→#509cf5).

```jsx
import { ProgressBar } from './ProgressBar.jsx';
<ProgressBar value={50} width={740} />
```
