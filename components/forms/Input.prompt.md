DFNS labelled input: 36px field, radius 6, white bg, inset 1px #c5c9de ring, 14px Suisse Intl.

```jsx
import { Input } from './Input.jsx';
<Input label="Email" placeholder="you@company.com" action={<a href="#" style={{fontSize:14}}>Forgot your password?</a>} />
```

Props: `iconLeft`/`iconRight` (16px icons), `action` (right-aligned label link), `disabled` (grey fill, muted text).
