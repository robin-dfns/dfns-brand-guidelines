The only permitted brand mark. Never redraw or recolour outside the token palette.

```jsx
<Logotype height={28} />
<Logotype height={32} showType={false} color="var(--colors-steel-grey-white)" />
```

The mask URLs resolve relative to `components/brand/`. Copy `assets/logo-symbol.svg` and `assets/logo-type.svg` alongside and adjust the paths when using the component outside this project.
