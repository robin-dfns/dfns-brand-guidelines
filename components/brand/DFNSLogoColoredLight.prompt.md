The second lockup the file defines. `Logotype` is the primary mark; use this one only where the source uses it.

```jsx
<DFNSLogoColoredLight height={36} />
<DFNSLogoColoredLight height={40} showType={false} color="var(--colors-ultra-purple-700)" />
```
